name: schedule
on:
  push:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"
jobs:
  schedule:
    runs-on: ubuntu-latest
    steps:
      - name: init
        run: |
          echo "${{ github.event.repository.name }}"
          echo $GITHUB_REPOSITORY
          echo $REPO
          echo "${{ toJson(github) }}"
